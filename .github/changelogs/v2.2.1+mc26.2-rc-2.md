# v2.2.1

## Fixed

- Now loads on **Minecraft 26.2-rc.2**. The previous build pinned the Fabric
  dependency to `26.2-rc-2` (the build-artifact name) instead of the in-game version id
  Fabric checks at runtime (`26.2-rc.2`), so the loader rejected the mod on launch.
