## Fixed
- The **Lonsdaleite Chestplate** and **Perfect Lonsdaleite Chestplate** are now craftable. Their recipes referenced a non-existent item id (`_chest` instead of `_chestplate`), so the game rejected them at load — they were the only recipes still broken after the 2.1.0 crafting-directory fix.
